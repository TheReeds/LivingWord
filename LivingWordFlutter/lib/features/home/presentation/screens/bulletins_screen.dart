import 'package:flutter/material.dart';

class BulletinsScreen extends StatelessWidget {
  const BulletinsScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Bulletins Screen'),
    );
  }
}